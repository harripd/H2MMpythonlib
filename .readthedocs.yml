version: 2

build:
  os: ubuntu-26.04
  tools:
    python: "3.12"

sphinx:
  builder: html
  configuration: docs/source/conf.py

python:
  install:
    - requirements: docs/source/requirements.txt
    - method: pip
      path: .
  # system_packages: true
