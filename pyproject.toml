[project]
requires-python = ">=3.6"
requires = "numpy>=1.20.1"

[build-system]
requires = [
    "setuptools>=42",
    "wheel",
    "cython",
    "numpy"
]
build-backend = "setuptools.build_meta"

[tool.cibuildwheel]
build = "*"
skip = "*musllinux*"
