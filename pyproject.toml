[build-system]
requires = [
    "setuptools>=42",
    "wheel",
    "cython",
    "IPython",
    "oldest-supported-numpy"
]
build-backend = "setuptools.build_meta"

[tool.cibuildwheel]
build = "*"
skip = "*-win32 *musllinux*"
