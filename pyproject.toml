[build-system]
requires = [
    "setuptools>=42",
    "wheel",
    "cython",
    "IPython",
    "numpy==1.20.1"
]
build-backend = "setuptools.build_meta"

[tool.cibuildwheel]
build = "*"
skip = "*musllinux*"